# Supabase Proxy Environment Configuration
# Copy this file to .env and update values as needed

# The frontend domain(s) allowed for CORS
# Allows any *.FRONTEND_DOMAIN origin
FRONTEND_DOMAIN=flagestrator.com

# Enable CORS for localhost (1 = yes, 0 = no)
# Useful for local development
ENABLE_LOCALHOST_CORS=1
